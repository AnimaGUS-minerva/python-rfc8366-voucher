alias t='make clean && make test'  # use 'build' as cache
alias tt='rm -rf build src/*/*.c src/*/*/*.c && t'
alias ttt='rm -rf local build src/*/*.c src/*/*/*.c && t'
alias r='echo todo REPL'
alias m='mt'
